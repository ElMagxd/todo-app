import React from 'react';

const Sidebar = () => {
   return (
      <aside className="sidebar">
         <button className="sidebar__item">All</button>
      </aside>
   );
};

export default Sidebar;